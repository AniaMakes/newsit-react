import React from 'react';
import PropTypes from 'prop-types';

const NavBar = ({}) => {
	return (
		<div>
			<button>Home</button>
			<button>Customise</button>
		</div>);
};



export default NavBar;
